version: 2
updates:
  - package-ecosystem: npm
    directory: '/'
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    ignore:
      # TypeScript 7.x is a compiler rewrite; stay on the 5.x major.
      - dependency-name: typescript
        update-types: ['version-update:semver-major']
      # The extension host runs VS Code's bundled Node, not the latest release,
      # so don't let @types/node drift a major ahead of the supported runtime.
      - dependency-name: '@types/node'
        update-types: ['version-update:semver-major']
    groups:
      dev-dependencies:
        dependency-type: development
      production-dependencies:
        dependency-type: production
    commit-message:
      prefix: deps
      prefix-development: deps-dev

  - package-ecosystem: github-actions
    directory: '/'
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
